project(CBU_IEEE_ROBOTICS_2024_TIMER)

set(SOURCE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/src)
set(SOURCE_FILES ${SOURCE_DIR}/timer.cpp)

add_library(CBU_IEEE_ROBOTICS_2024_TIMER ${SOURCE_FILES})

target_include_directories(CBU_IEEE_ROBOTICS_2024_TIMER PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/include)
